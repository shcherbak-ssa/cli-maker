'use strict';

const PROJECT_ID_TYPE = 'string';
const MAX_PROJECT_ID_LENGTH = 30;
const PROJECT_ID_PATTERN = '[-\w]+';

module.exports = {
  PROJECT_ID_TYPE,
  MAX_PROJECT_ID_LENGTH,
  PROJECT_ID_PATTERN
};